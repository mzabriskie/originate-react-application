import React from 'react';
import Router from 'react-router';
import routes from './app/routes';

Router.run(routes, function (Handler) {
  React.render(<Handler/>, document.getElementById('react-application-root'));
});
